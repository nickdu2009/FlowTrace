## 日期：2026-02-14

### 今日目标（Goal）
- 将项目从 Android-only 的文档基线，扩展为 Android + iOS 双平台的文档基线与技术选型。
- 审视技术方案、修复代码骨架中的结构性问题、补全缺失端口接口。

### 关联需求/验收（Traceability）
- FR（Android）：见 `doc/ANDROID_MVP_REQUIREMENTS.md`
- FR（iOS）：见 `doc/IOS_MVP_REQUIREMENTS.md`
- AC（Android）：`AC-01..AC-05`
- AC（iOS）：`AC-iOS-01..AC-iOS-05`
- ADR：
  - `doc/adr/ADR-0001-mvp-decisions.md`
  - `doc/adr/ADR-0002-ios-platform-strategy.md`
  - `doc/adr/ADR-0003-shared-core-kmp.md`

### 变更摘要（What changed）

#### 文档变更（上午）
- 新增 `doc/IOS_MVP_REQUIREMENTS.md`：iOS MVP 范围与验收，明确与 Android 差异（尤其 FR-02）
- 新增 `doc/adr/ADR-0002-ios-platform-strategy.md`：iOS 平台策略（NetworkExtension Packet Tunnel、App Group、iOS 15+）
- 新增 `doc/adr/ADR-0003-shared-core-kmp.md`：共享核心（KMP）与模块边界
- 更新 `doc/TECH_STACK.md`：扩展为 Android+iOS 技术栈与 shared-core 模块划分
- 更新 `doc/MVP_IMPLEMENTATION_PLAN.md`：新增 iOS 里程碑与 shared-core 前置
- 更新 `doc/README.md`：索引新增 iOS 需求与 ADR
- 更新 `AGENTS.md`：事实来源新增 iOS 需求；硬约束新增 iOS 基线与 shared-core

#### 代码骨架修复（下午——技术方案审视）

**问题 1（严重）：包名路径不一致**
- 文件系统目录为 `com/myfiddler/`，但所有 `.kt` 文件 package 声明为 `com.flowtrace.*`
- 修复：将所有源码文件迁移到 `com/flowtrace/` 目录下，删除空的 `com/myfiddler/` 目录树

**问题 2（严重）：`settings.gradle.kts` 内容重复**
- `pluginManagement`/`dependencyResolutionManagement`/`include` 各出现两次
- 修复：去重，保留一份

**问题 3（严重）：`WsDirection` 枚举重复定义**
- `CaptureEvent.kt` 和 `SessionModels.kt` 各自定义了 `WsDirection`，类型不互通
- 修复：统一定义到 `domain.model.Identifiers.kt`，两处改为 import 引用

**问题 4（严重）：`CaptureEngine.kt` 中 `SessionId` import 错误**
- 错误地 import `com.flowtrace.domain.session.SessionId`，实际定义在 `domain.model`
- 修复：改为 `import com.flowtrace.domain.model.SessionId`

**问题 5（中等）：缺少 ADR-0003 承诺的端口接口**
- 新增 `BodyStorePort`（`domain/session/BodyStorePort.kt`）：Body 分离存储端口
- 新增 `ClockPort`（`domain/model/ClockPort.kt`）：时间源端口，便于测试与一致性
- 新增 `SessionAggregator`（`domain/capture/SessionAggregator.kt`）：会话聚合器端口，幂等合并、乱序容忍、批处理友好
- 新增 `CaptureState`（`domain/capture/CaptureEngine.kt`）：VPN 抓包状态机枚举（Idle/Starting/Running/Stopping/Error）

**问题 6（中等）：HTTP Headers 用 `Map<String, String>` 建模，丢失重复 header**
- HTTP 规范允许同名 header 多次出现（典型如 `Set-Cookie`）
- 修复：`CaptureEvent`、`SessionModels`、`Redactor` 中的 headers 统一改为 `List<Pair<String, String>>`

**问题 7（轻微）：缺少 Timing 结构化模型**
- 新增 `Timing`（`domain/capture/Timing.kt`）：dns/connect/tls/ttfb/download 分阶段耗时
- `HttpResponseCompleted` 和 `SessionDetail` 中新增 `timing: Timing?` 字段

### 关键实现细节（How）
- 架构/边界：
  - 继续遵循 Clean Architecture（Domain/Application/Infra/Presentation）与 Ports/Adapters 模式
  - 所有新增接口（BodyStorePort/ClockPort/SessionAggregator）都定义在 Domain 层，不依赖平台 SDK
  - CaptureState 定义在 CaptureEngine 同文件中，作为引擎状态的一部分
- 安全/脱敏：
  - Redactor 接口签名更新为 `List<Pair<String, String>>`，脱敏策略不变
- 性能/背压：
  - SessionAggregator 接口设计为批处理友好（`ingest(events: List<CaptureEvent>)`）

### 验证（Verification）
- 手工校验：
  - `find . -path '*/com/myfiddler*' -type f` 返回空（旧目录已清除）
  - `grep -r 'import com.myfiddler'` 返回空（无残留引用）
  - 所有 `.kt` 文件 package 声明与目录路径一致
  - `settings.gradle.kts` 无重复块
  - `WsDirection` 仅在 `Identifiers.kt` 中定义一次

### 问题与决策（Issues & Decisions）
- 决策：
  - 包名统一使用 `com.flowtrace.*`（与 rootProject.name = "flowtrace-android" 对齐）
  - Headers 统一使用 `List<Pair<String, String>>` 而非 `Map`（支持 HTTP 同名 header）
  - `WsDirection` 提取到 `domain.model` 包作为共享类型

### 风险与后续（Risks & Next）
- 风险：
  - iOS NetworkExtension Entitlement 与审核不确定性，可能影响分发策略
- 后续：
  - 初始化 KMP Shared Core 模块（将 Domain + Application + Event + Redactor + Export 迁移到 `shared/`）
  - 搭建 Android Gradle 构建系统（根 build.gradle.kts、模块 build.gradle.kts、Gradle Wrapper）
  - SunnyNet 最小链路验证（Android 真机跑通 VPN → 回调 → 日志）
  - 尽早做 iOS "最小可运行 Extension"样例验证签名、Entitlement 与隧道启停链路
